---
Tabs_1:
  activeTab: 0
  Card_title:
    Header:
      title: A registration form
      Button_1:
        brand: twitter
        label: shankar
        onClick: >
          //thisContext().setState('tabs.tab3.label.label', "joey");

          thisContext().run("m1.js", {test: "joey hahaha account"}); 

        color: success
        position: left
      Button_2:
        brand: facebook
        label: facebook
        onClick: >

          thisContext().kill();

        color: success
        position: right
    Form:
      Field_1:
        type: text
        label: Username
        tooltip: Choose your username
        defaultValue: First name
        prepend:
          - type: button
            icon: icon-drop
            click: next.yaml
            color: warning
        append:
          - type: text
            icon: icon-drop
            click: next.yaml
            label: hajajaj
            color: success
      Field_2:
        type: password
        label: Password
      Field_3:
        type: date
        label: Date
        tooltip: Please Input Your Birthday
      Field_4:
        type: switch
        label: Remember Me
      Button_submit:
        type: submit
        click: next.yaml
        brand: youtube
        color: warning
        onClick: >

          thisContext().notify('Notification title' + Math.random(),'Some text', ()=> thisContext().shell.openExternal("http://google.com"));

        size: lg
        _className: 'mr-3'
      Button_cancel:
        type: button
        label: cancel
        icon: icon-drop
        click: next.yaml
        color: warning
  Card_second:
      Header:
        title: A second registration form
        Button_1:
          brand: twitter
          label: shankar
          onclick: >
            thisContext().setState('tabs.tab3.label.label', "joey");
            //thisContext().run('m1.js');
          color: success
          position: left
        Button_2:
          brand: facebook
          label: facebook
          onClick: >
            thisContext().kill();
          color: success
          position: right
      Markdown:
        content: >
          # asjkdhsakdhakjshdakjdhjkashdkjashdkjlashdkjlashdkjsahdjksahdjkash'

          ## 11

          ### 33

          ###### **33**
          <h2>Full example</h2>

          ``` jsx   

            var start = new Date().getTime();
            setInterval(function() {
              React.render(
                &lt;ExampleApplication elapsed={new Date().getTime() - start} />,
                document.getElementById('container')
              );
            }, 50);
            
          ```
            ---

          Which one does @torvalds like most?
            - [x] tiger \#
            - [ ] whale \*
            - [X] octocat \!

          ```scss
            @include text-color-variant(".text-primary", $primary-color);
            @include text-color-variant(".text-secondary", $secondary-color-dark);
            @include text-color-variant(".text-gray", $gray-color);
            @include text-color-variant(".text-light", $light-color);
            @include text-color-variant(".text-success", $success-color);
            @include text-color-variant(".text-warning", $warning-color);
            @include text-color-variant(".text-error", $error-color);
          ```

          This block of <kbd>cd</kbd> Markdown contains <a href="https://en.wikipedia.org/wiki/HTML">HTML</a>, and will require the <code>html-parser</code> AST plugin to be loaded, in addition to setting the <code class="prop">escapeHtml</code> property to false.

      Form:
        Field_1:
          type: text
          label: Username
          tooltip: Choose your username
          defaultValue: First name
          prepend:
            - type: button
              icon: icon-drop
              click: next.yaml
              color: warning
          append:
            - type: text
              icon: icon-drop
              click: next.yaml
              label: hajajaj
              color: success
        Field_2:
          type: password
          label: Password
        Field_3:
          type: date
          label: Date
          tooltip: Please Input Your Birthday
        Button_submit:
          type: submit
          click: next.yaml
          brand: youtube
          color: warning
          onClick: >

            thisContext().notify('Notification title' + Math.random(),'Some text', ()=> thisContext().shell.openExternal("http://google.com"));

          size: lg
          _className: 'mr-3'
        Button_cancel:
          type: button
          label: cancel
          icon: icon-drop
          click: next.yaml
          color: warning
