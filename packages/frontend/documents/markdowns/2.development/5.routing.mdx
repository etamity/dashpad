export const config = {
    menu: 'Routing',
    icon: 'icon-share'
};


## Routing

---

When we created new plugin, it will automatically create new route for we.
For example when we created a new plugin under `src/plugins/NewPlugin`, and export the plugin in the `src/plugins/index.js` file:

`export * from './NewPlugin';`

it will generate a new route `/newplugin`.